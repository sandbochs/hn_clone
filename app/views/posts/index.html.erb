<% @posts.each do |post| %>

<p><%= link_to post.title, post.article_url %> | <%= link_to '(comments)', post_comments_path(post) %>
	<% if can? :edit, post %>
	 | <%= link_to '(edit)', edit_post_path(post) %>
	<% end %>
</p>
<% end %>

<p><%= paginate @posts %></p>